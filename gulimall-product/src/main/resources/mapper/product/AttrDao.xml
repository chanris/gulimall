<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chanris.gulimall.product.dao.AttrDao">

    <resultMap type="com.chanris.gulimall.product.entity.AttrEntity" id="attrMap">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="searchType" column="search_type"/>
        <result property="valueType" column="value_type"/>
        <result property="icon" column="icon"/>
        <result property="valueSelect" column="value_select"/>
        <result property="attrType" column="attr_type"/>
        <result property="enable" column="enable"/>
        <result property="catelogId" column="catelog_id"/>
        <result property="showDesc" column="show_desc"/>
    </resultMap>

    <select id="pageWithAttrGroupId" resultType="com.chanris.gulimall.product.dto.AttrDTO">
        select pa.*, paar.attr_group_id from pms_attr pa left join pms_attr_attrgroup_relation paar on pa.attr_id = paar.attr_id
        where 1 = 1
        <if test="catelogId != null">
            and pa.catelog_id = #{catelogId}
        </if>
        <if test="attrType != null">
            and pa.attr_type = #{attrType}
        </if>
    </select>

    <select id="getWithAttrGroupId" resultType="com.chanris.gulimall.product.dto.AttrDTO">
        select pms_attr.* , paar.attr_group_id from pms_attr left join pms_attr_attrgroup_relation paar on pms_attr.attr_id = paar.attr_id
        where pms_attr.attr_id = #{val}
    </select>
</mapper>